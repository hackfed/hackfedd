[general]
enabled = yes
servername = Asterisk
; Keep AMI-over-HTTP local when hackfedd runs on this host. If it does not,
; bind only to a trusted management address and enforce equivalent firewalling.
bindaddr = 127.0.0.1
bindport = 8088
