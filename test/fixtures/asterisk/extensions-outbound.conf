; This file is auto-generated by hackfedd.
; DO NOT EDIT THIS FILE MANUALLY. Your changes will be lost.

[hackfed-outbound]
; Accept canonical E.164 dial strings and normalize them for internal routing
exten => _+7509101.,1,Goto(hackfed-outbound,${EXTEN:1},1)
exten => _+7509008!,1,Goto(hackfed-outbound,${EXTEN:1},1)
exten => _+12025550123!,1,Goto(hackfed-outbound,${EXTEN:1},1)

; Calls to the local Hackfed prefix
exten => _7509101.,1,Dial(PJSIP/${EXTEN:7},30,rT)
 same => n,Hangup()

; Calls to (bksp) B4CKSP4CE
exten => _7509008!,1,GotoIf($["${HACKFED_INBOUND}" = "1"]?invalid)
 same => n,Set(HF_DESTINATION=${FILTER(0-9,${EXTEN})})
 same => n,GotoIf($[${LEN(${HF_DESTINATION})} = ${LEN(${EXTEN})}]?valid-destination:invalid)
 same => n(valid-destination),Set(HF_CALLER_RAW=${CALLERID(num)})
 same => n,GotoIf($["${HF_CALLER_RAW:0:1}" = "+"]?caller-strip-plus)
 same => n,Set(HF_CALLER_HAD_PLUS=0)
 same => n,Set(HF_CALLER_BODY=${HF_CALLER_RAW})
 same => n,Goto(caller-validate)
 same => n(caller-strip-plus),Set(HF_CALLER_HAD_PLUS=1)
 same => n,Set(HF_CALLER_BODY=${HF_CALLER_RAW:1})
 same => n(caller-validate),Set(HF_CALLER_DIGITS=${FILTER(0-9,${HF_CALLER_BODY})})
 same => n,GotoIf($[${LEN(${HF_CALLER_DIGITS})} = ${LEN(${HF_CALLER_BODY})} & ${LEN(${HF_CALLER_DIGITS})} > 0]?caller-digits:invalid)
 same => n(caller-digits),GotoIf($["${HF_CALLER_DIGITS:0:7}" = "7509101"]?caller-complete)
 same => n,GotoIf($[${HF_CALLER_HAD_PLUS} = 1]?invalid)
 same => n,GotoIf($["${HF_CALLER_DIGITS:0:7}" = "7509008"]?invalid)
 same => n,GotoIf($["${HF_CALLER_DIGITS:0:11}" = "12025550123"]?invalid)
 same => n,Set(HF_CALLER_DIGITS=7509101${HF_CALLER_DIGITS})
 same => n(caller-complete),Set(CALLERID(num)=+${HF_CALLER_DIGITS})
 same => n,Dial(IAX2/hf-bksp-primary-47dc6da5895a/${HF_DESTINATION},30,rT)
 same => n,Hangup()
 same => n(invalid),Hangup(28)

; Calls to (fab20) FAB20 include evil SHELL id
exten => _12025550123!,1,GotoIf($["${HACKFED_INBOUND}" = "1"]?invalid)
 same => n,Set(HF_DESTINATION=${FILTER(0-9,${EXTEN})})
 same => n,GotoIf($[${LEN(${HF_DESTINATION})} = ${LEN(${EXTEN})}]?valid-destination:invalid)
 same => n(valid-destination),Set(HF_CALLER_RAW=${CALLERID(num)})
 same => n,GotoIf($["${HF_CALLER_RAW:0:1}" = "+"]?caller-strip-plus)
 same => n,Set(HF_CALLER_HAD_PLUS=0)
 same => n,Set(HF_CALLER_BODY=${HF_CALLER_RAW})
 same => n,Goto(caller-validate)
 same => n(caller-strip-plus),Set(HF_CALLER_HAD_PLUS=1)
 same => n,Set(HF_CALLER_BODY=${HF_CALLER_RAW:1})
 same => n(caller-validate),Set(HF_CALLER_DIGITS=${FILTER(0-9,${HF_CALLER_BODY})})
 same => n,GotoIf($[${LEN(${HF_CALLER_DIGITS})} = ${LEN(${HF_CALLER_BODY})} & ${LEN(${HF_CALLER_DIGITS})} > 0]?caller-digits:invalid)
 same => n(caller-digits),GotoIf($["${HF_CALLER_DIGITS:0:7}" = "7509101"]?caller-complete)
 same => n,GotoIf($[${HF_CALLER_HAD_PLUS} = 1]?invalid)
 same => n,GotoIf($["${HF_CALLER_DIGITS:0:7}" = "7509008"]?invalid)
 same => n,GotoIf($["${HF_CALLER_DIGITS:0:11}" = "12025550123"]?invalid)
 same => n,Set(HF_CALLER_DIGITS=7509101${HF_CALLER_DIGITS})
 same => n(caller-complete),Set(CALLERID(num)=+${HF_CALLER_DIGITS})
 same => n,Dial(IAX2/hf-fab20-primary-0b6a968f40b6/${HF_DESTINATION},30,rT)
 same => n,Hangup()
 same => n(invalid),Hangup(28)

